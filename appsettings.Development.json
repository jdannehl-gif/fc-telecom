/* FC Telecom Manager
   ------------------------------------------------------------------------------
   Deliberately plain CSS: no framework, no build step, no external font. This is a
   tool people open during an outage, sometimes on a phone with one bar, and every
   dependency is another thing that can fail at exactly the wrong moment.

   Colour is never the only signal. Every status carries an icon and a word too. */

:root {
    --ink: #12263a;
    --ink-muted: #5b6b7c;
    --line: #d9e0e7;
    --surface: #ffffff;
    --surface-alt: #f4f7fa;
    --brand: #1f4e79;
    --brand-dark: #0d2b45;
    --ok: #1c6b45;
    --ok-bg: #e6f4ec;
    --warn: #8a6d08;
    --warn-bg: #fdf5db;
    --urgent: #a02020;
    --urgent-bg: #fbeaea;
    --unknown: #5b6b7c;
    --unknown-bg: #eceff2;
    --radius: 6px;
    --shadow: 0 1px 2px rgba(18, 38, 58, .08);
}

@media (prefers-color-scheme: dark) {
    :root {
        --ink: #e8eef5;
        --ink-muted: #9fb0c0;
        --line: #2b3a49;
        --surface: #16212c;
        --surface-alt: #101922;
        --brand: #4a7ba7;
        --ok-bg: #14301f;
        --warn-bg: #332a08;
        --urgent-bg: #351212;
        --unknown-bg: #1e2831;
    }
}

* { box-sizing: border-box; }

body {
    margin: 0;
    font-family: -apple-system, BlinkMacSystemFont, "Segoe UI", Roboto, "Helvetica Neue", Arial, sans-serif;
    font-size: 15px;
    line-height: 1.5;
    color: var(--ink);
    background: var(--surface-alt);
}

a { color: var(--brand); }
code { font-family: ui-monospace, "SF Mono", Menlo, Consolas, monospace; font-size: .92em; }

.sr-only {
    position: absolute; width: 1px; height: 1px; padding: 0; margin: -1px;
    overflow: hidden; clip: rect(0, 0, 0, 0); white-space: nowrap; border: 0;
}

/* Focus is visible everywhere. Keyboard navigation is not optional in a tool that
   people use one-handed while holding a phone to their ear. */
:focus-visible {
    outline: 3px solid var(--brand);
    outline-offset: 2px;
}

/* ── Shell ─────────────────────────────────────────────────────────────────── */

.app-shell { min-height: 100vh; display: flex; flex-direction: column; }

.app-header {
    display: flex; align-items: center; gap: 1rem;
    padding: .6rem 1rem;
    background: var(--brand-dark); color: #fff;
}

.app-brand { color: #fff; text-decoration: none; font-weight: 600; white-space: nowrap; }
.app-user { margin-left: auto; display: flex; gap: .75rem; align-items: center; font-size: .9rem; }
.app-user__signout { color: #cfe0ef; }

.app-body { display: flex; flex: 1; min-height: 0; }

.app-nav { width: 200px; background: var(--surface); border-right: 1px solid var(--line); padding: 1rem 0; }
.app-nav ul { list-style: none; margin: 0; padding: 0; }
.app-nav a {
    display: block; padding: .5rem 1rem; color: var(--ink); text-decoration: none;
    border-left: 3px solid transparent;
}
.app-nav a:hover { background: var(--surface-alt); }
.app-nav a.active { border-left-color: var(--brand); background: var(--surface-alt); font-weight: 600; }

.app-main { flex: 1; padding: 1.25rem 1.5rem 3rem; min-width: 0; }

h1 { font-size: 1.5rem; margin: 0 0 .25rem; }
h2 { font-size: 1.05rem; margin: 0 0 .75rem; }
h3 { font-size: .95rem; margin: 1rem 0 .35rem; }

.page-head { display: flex; align-items: flex-start; gap: 1rem; margin-bottom: 1rem; }
.page-actions { margin-left: auto; display: flex; gap: .5rem; }
.breadcrumb { margin-bottom: .5rem; font-size: .9rem; }
.record-head { margin-bottom: 1rem; }
.record-subtitle { color: var(--ink-muted); margin: .25rem 0; }
.record-meta { display: flex; flex-wrap: wrap; gap: .75rem; font-size: .9rem; color: var(--ink-muted); }

.muted { color: var(--ink-muted); }
.numeric { text-align: right; font-variant-numeric: tabular-nums; }
.loading { color: var(--ink-muted); font-style: italic; }

/* ── Search ────────────────────────────────────────────────────────────────── */

.search { position: relative; flex: 1; max-width: 560px; }
.search__input {
    width: 100%; padding: .45rem .7rem; border-radius: var(--radius);
    border: 1px solid transparent; font-size: .95rem;
}
.search__results {
    position: absolute; z-index: 20; top: calc(100% + .3rem); left: 0; right: 0;
    background: var(--surface); border: 1px solid var(--line); border-radius: var(--radius);
    box-shadow: var(--shadow); list-style: none; margin: 0; padding: .25rem; max-height: 60vh; overflow: auto;
}
.search__results a { display: grid; gap: .1rem; padding: .45rem .6rem; text-decoration: none; color: var(--ink); }
.search__results a:hover { background: var(--surface-alt); }
.search__kind { font-size: .72rem; text-transform: uppercase; letter-spacing: .04em; color: var(--ink-muted); }
.search__title { font-weight: 600; }
.search__subtitle { font-size: .85rem; color: var(--ink-muted); }
.search__none { padding: .6rem; color: var(--ink-muted); font-size: .9rem; }

/* ── Panels and tiles ──────────────────────────────────────────────────────── */

.panel {
    background: var(--surface); border: 1px solid var(--line); border-radius: var(--radius);
    padding: 1rem; margin-bottom: 1rem; box-shadow: var(--shadow);
}
.panel-row { display: grid; grid-template-columns: repeat(auto-fit, minmax(340px, 1fr)); gap: 1rem; }

.tiles { display: grid; grid-template-columns: repeat(auto-fit, minmax(180px, 1fr)); gap: 1rem; margin-bottom: 1rem; }
.tile {
    background: var(--surface); border: 1px solid var(--line); border-radius: var(--radius);
    padding: 1rem; display: grid; gap: .25rem; box-shadow: var(--shadow);
}
.tile__value { font-size: 1.6rem; font-weight: 600; font-variant-numeric: tabular-nums; }
.tile__label { font-size: .85rem; color: var(--ink-muted); }

.headline-metric { font-size: 1.6rem; font-weight: 600; margin: 0 0 .5rem; }
.headline-metric__qualifier { font-size: .85rem; font-weight: 400; color: var(--ink-muted); display: block; }

/* ── Attention list ────────────────────────────────────────────────────────── */

.attention-list { list-style: none; margin: 0; padding: 0; display: grid; gap: .4rem; }
.attention {
    display: flex; align-items: baseline; gap: .6rem;
    padding: .5rem .7rem; border-radius: var(--radius); border-left: 4px solid var(--line);
    background: var(--surface-alt);
}
.attention--urgent { border-left-color: var(--urgent); background: var(--urgent-bg); }
.attention--warning { border-left-color: var(--warn); background: var(--warn-bg); }
.attention__count { font-weight: 700; font-variant-numeric: tabular-nums; }
.attention__link { margin-left: auto; }

/* ── Banners ───────────────────────────────────────────────────────────────── */

.banner { border-radius: var(--radius); padding: .85rem 1rem; margin-bottom: 1rem; border-left: 5px solid; }
.banner h2 { margin: 0 0 .35rem; font-size: 1rem; }
.banner--warn { background: var(--warn-bg); border-left-color: var(--warn); }
.banner--info { background: var(--unknown-bg); border-left-color: var(--unknown); }
.banner ul { margin: .4rem 0 0; padding-left: 1.1rem; }
.warning { color: var(--warn); font-weight: 500; }

/* ── Tables ────────────────────────────────────────────────────────────────── */

table { width: 100%; border-collapse: collapse; font-size: .92rem; }
th, td { text-align: left; padding: .45rem .6rem; border-bottom: 1px solid var(--line); vertical-align: top; }
thead th { font-size: .78rem; text-transform: uppercase; letter-spacing: .04em; color: var(--ink-muted); }
tbody tr:hover { background: var(--surface-alt); }
.row--alert { background: var(--urgent-bg); }

.bar-table td.bar-cell { width: 45%; }
.bar { display: block; height: .7rem; background: var(--brand); border-radius: 2px; min-width: 2px; }

.kv { display: grid; grid-template-columns: minmax(140px, auto) 1fr; gap: .3rem .9rem; margin: 0; }
.kv dt { color: var(--ink-muted); font-size: .88rem; }
.kv dd { margin: 0; }

/* ── Chips and badges ──────────────────────────────────────────────────────── */

.chip, .badge {
    display: inline-flex; align-items: center; gap: .28rem;
    padding: .1rem .45rem; border-radius: 999px; font-size: .78rem; font-weight: 600;
    background: var(--unknown-bg); color: var(--unknown); white-space: nowrap;
}
.chip--up, .badge--ok { background: var(--ok-bg); color: var(--ok); }
.chip--down, .badge--urgent, .badge--critical { background: var(--urgent-bg); color: var(--urgent); }
.chip--warn, .badge--warn, .badge--high { background: var(--warn-bg); color: var(--warn); }
.chip--unknown, .badge--unknown { background: var(--unknown-bg); color: var(--unknown); }
.chip--muted, .badge--muted { background: var(--unknown-bg); color: var(--ink-muted); font-weight: 400; }
.badge--restricted { background: var(--warn-bg); color: var(--warn); }

/* ── Sensitive values ──────────────────────────────────────────────────────── */

.sensitive { display: inline-flex; align-items: center; gap: .4rem; }
.sensitive__mask { letter-spacing: .12em; color: var(--ink-muted); }
.sensitive__toggle {
    font-size: .75rem; padding: .05rem .4rem; border: 1px solid var(--line);
    background: var(--surface); border-radius: var(--radius); cursor: pointer; color: var(--brand);
}

/* ── Service cards ─────────────────────────────────────────────────────────── */

.service-cards { list-style: none; margin: 0; padding: 0; display: grid; gap: .75rem; }
.service-card { border: 1px solid var(--line); border-radius: var(--radius); padding: .75rem; background: var(--surface); }
.service-card__head { display: flex; align-items: center; gap: .6rem; flex-wrap: wrap; margin-bottom: .4rem; }
.service-card__role { font-size: .72rem; font-weight: 700; letter-spacing: .08em; color: var(--ink-muted); }
.service-card__type { font-weight: 600; }
.service-card__link { margin-left: auto; font-size: .85rem; }
.service-card__body p { margin: .2rem 0; font-size: .9rem; }
.service-card__warning { color: var(--warn); font-weight: 500; }

.deadline-list { list-style: none; margin: 0; padding: 0; display: grid; gap: .35rem; font-size: .9rem; }
.deadline-list li { display: flex; gap: .6rem; align-items: baseline; flex-wrap: wrap; }
.deadline-list time { font-variant-numeric: tabular-nums; color: var(--ink-muted); }

.role-usage { list-style: none; margin: .25rem 0 0; padding: 0; font-size: .9rem; }
.procedure { border-top: 1px solid var(--line); padding-top: .75rem; margin-top: .75rem; }
.procedure__steps { white-space: pre-wrap; background: var(--surface-alt); padding: .6rem; border-radius: var(--radius); font-size: .85rem; }

/* ── Filters, buttons, pager ───────────────────────────────────────────────── */

.filters { display: flex; flex-wrap: wrap; gap: .5rem; align-items: center; margin-bottom: 1rem; }
.filters input[type="search"], .filters select {
    padding: .35rem .55rem; border: 1px solid var(--line); border-radius: var(--radius);
    background: var(--surface); color: var(--ink); font-size: .9rem;
}
.filters input[type="search"] { min-width: 260px; }
.check { display: inline-flex; align-items: center; gap: .3rem; font-size: .9rem; }

.button {
    display: inline-block; padding: .38rem .8rem; border-radius: var(--radius);
    background: var(--brand); color: #fff; text-decoration: none; border: 1px solid var(--brand);
    font-size: .9rem; cursor: pointer;
}
.button--secondary { background: var(--surface); color: var(--brand); }
.button--tertiary { background: transparent; border-color: transparent; color: var(--brand); }

.pager { display: flex; gap: 1rem; align-items: center; justify-content: center; margin-top: 1rem; font-size: .9rem; }
.pager button {
    padding: .3rem .7rem; border: 1px solid var(--line); background: var(--surface);
    border-radius: var(--radius); cursor: pointer; color: var(--ink);
}
.pager button:disabled { opacity: .45; cursor: default; }

.empty-state { text-align: center; padding: 2rem 1rem; color: var(--ink-muted); }
.empty-state__title { font-weight: 600; color: var(--ink); margin: 0 0 .3rem; }
.empty-state__explanation { margin: 0 0 .8rem; }

.page-message { max-width: 640px; margin: 3rem auto; text-align: center; }
.page-message--denied h1, .page-message--error h1 { color: var(--urgent); }

/* ── Blazor reconnect banner ───────────────────────────────────────────────── */

#blazor-error-ui {
    display: none; position: fixed; bottom: 0; left: 0; right: 0; z-index: 1000;
    background: var(--warn-bg); color: var(--warn); padding: .7rem 1rem;
    border-top: 2px solid var(--warn); text-align: center;
}
#blazor-error-ui .dismiss { cursor: pointer; float: right; }

/* ── Phone ─────────────────────────────────────────────────────────────────── */

@media (max-width: 780px) {
    .app-body { flex-direction: column; }
    .app-nav { width: 100%; border-right: none; border-bottom: 1px solid var(--line); padding: .25rem 0; }
    .app-nav ul { display: flex; overflow-x: auto; }
    .app-nav a { border-left: none; border-bottom: 3px solid transparent; white-space: nowrap; }
    .app-nav a.active { border-left: none; border-bottom-color: var(--brand); }
    .app-main { padding: 1rem .75rem 3rem; }
    .app-header { flex-wrap: wrap; }
    .search { order: 3; flex-basis: 100%; max-width: none; }

    /* Wide tables become stacked cards rather than a horizontal scroll nobody finds.
       This matters most on the outage view, which is explicitly a phone-first screen. */
    .data-table thead { display: none; }
    .data-table tr { display: block; border: 1px solid var(--line); border-radius: var(--radius); margin-bottom: .5rem; padding: .5rem; }
    .data-table td, .data-table th { display: block; border: none; padding: .15rem 0; text-align: left; }
    .numeric { text-align: left; }
}
